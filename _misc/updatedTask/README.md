With an updated Task list, the Excel document containing the case studies must be updated accordingly. 

Since there is no direct relationship between the old and new task list, an AI API must be used to bridge the gap. 

This will likely be imperfect, but will hopefully be >95% accurate. 

Previous tasks defined as 'Diverse' should be left as such.

This file assumes that the data has been decomposed via dataDev1, i.e., .txt files, not .pdf files.